generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}



// model User {
//   id         String   @id @default(uuid()) @db.Uuid
//   name       String   @db.VarChar(100)
//   email      String   @unique @db.VarChar(200)
//   password   String   @db.VarChar(200)
//   userType   String   @db.VarChar(50)

//   package    Package[]
// }

// model Package {
//   id          String  @id @default(uuid()) @db.Uuid
//   ETA         DateTime
//   weight      Float
//   fragile     Boolean
//   dispatchDate DateTime
//   notes       String? @db.VarChar(250)

//   userId      String  @db.Uuid
//   statusId    String  @db.Uuid
//   destinationId String @db.Uuid
//   locationId  String @db.Uuid
//   originId    String @db.Uuid

//   user        User    @relation(fields: [userId], references: [id]) 
//   status      PackageStatus @relation(fields: [statusId], references: [id])
//   destination Address @relation(fields: [destinationId], references: [id])
//   location    Location @relation("locationfk", fields: [locationId], references: [id])
//   origin      Location @relation("originfk", fields: [originId], references: [id])
// }

// model PackageStatus {
//   id          String  @id @default(uuid()) @db.Uuid
//   arrived     String  @db.VarChar(100)
//   canceled    String  @db.VarChar(100)
//   transit     String  @db.VarChar(100)

//   package     Package[]
// }

// model Location {
//   id          String  @id @default(uuid()) @db.Uuid
//   name        String @db.VarChar(150)

//   location    Package[] @relation("locationfk")
//   origin      Package[] @relation("originfk") 

// }

// model Address {
//   id          String  @id @default(uuid()) @db.Uuid
//   name        String @db.VarChar(150)

//   package     Package[]
// }





model usuarios {
  id         String   @id @default(uuid()) @db.Uuid
  name       String   @db.VarChar(100)
  lastname   String?  @db.VarChar(100) 
  email      String   @unique @db.VarChar(200)
  password   String   @db.VarChar(200)
  level      String   @db.VarChar(50)
  profile_image Bytes?
  default_picture String? @default("profile") @db.VarChar(50)
  accesos_directos Json? @db.JsonB
  created_at DateTime @default(now()) @db.Timestamptz
  subscriptions Suscripcion[] 
  passwordResetToken String?   @unique
  passwordResetExpires DateTime? 

  attempts login_attempts[]
  progreso ProgresoRecurso[]
  
  examenes             ExamenIntento[]
  examenCompletado     Boolean           @default(false)
}

model login_attempts {
  id          String    @id @default(uuid())
  usuarioid   String    @db.Uuid
  attemptedat DateTime  @default(now())
  success     Boolean
  ipaddress   String?
  useragent   String?
  reason      String?

  usuarios    usuarios  @relation(fields: [usuarioid], references: [id], onDelete: Cascade, onUpdate: Cascade)
}

model Plan {
  id      String   @id @default(uuid()) @db.Uuid
  name    String   @unique @db.VarChar(100)
  code    String @unique @db.VarChar(100)
  price   Float

  subscriptions Suscripcion[]
  examenes        Examen?
  examenesIntento ExamenIntento[]
  clasesBonus       ClaseBonus[]
}

model Suscripcion {
  id        String   @id @default(uuid()) @db.Uuid
  userId    String   @db.Uuid
  planId    String   @db.Uuid
  startDate DateTime @default(now()) @db.Timestamptz
  endDate   DateTime @db.Timestamptz
  status    String   @default("pending") @db.VarChar(50) // e.g., "active", "pending", "canceled"

  usuarios usuarios @relation(fields: [userId], references: [id])
  plan Plan @relation(fields: [planId], references: [id])

  @@unique([userId, planId])
}

model ExamenIntento {
  id           String    @id @default(uuid()) @db.Uuid
  usuarioId    String    @db.Uuid
  planId       String    @db.Uuid
  puntaje      Float?
  aprobado     Boolean   @default(false)
  fechaIntento DateTime  @default(now()) @db.Timestamptz

  usuario      usuarios @relation(fields: [usuarioId], references: [id])
  plan         Plan     @relation(fields: [planId], references: [id])
}


model Examen {
  id           String   @id @default(uuid()) @db.Uuid
  planCode     String   @unique @db.VarChar(100)
  plan         Plan     @relation(fields: [planCode], references: [code])
  preguntas    ExamenPregunta[]
}

model ExamenPregunta {
  id                     String           @id @default(uuid()) @db.Uuid
  pregunta               String           @db.VarChar(255)
  opciones               ExamenOpcion[]
  respuestaCorrecta      ExamenOpcion?     @relation("ExamenRespuestaCorrecta", fields: [respuestaCorrectaId], references: [id])
  respuestaCorrectaId    String?           @unique @db.Uuid // This field holds the ID of the correct option
  examenId               String           @db.Uuid
  examen                 Examen           @relation(fields: [examenId], references: [id])
}

model ExamenOpcion {
  id           String         @id @default(uuid()) @db.Uuid
  texto        String         @db.VarChar(255)
  preguntaId   String         @db.Uuid
  pregunta     ExamenPregunta @relation(fields: [preguntaId], references: [id])
  isCorrecta   ExamenPregunta? @relation("ExamenRespuestaCorrecta")
}

enum TipoTema {
  EMPRENDEDOR
  PERSONAL
}

model Tema {
  id          String      @id @default(uuid()) @db.Uuid
  nombre      String      @db.VarChar(100)
  descripcion String?
  orden       Int         
  tipo        TipoTema    @default(EMPRENDEDOR)
  
  recursos    Recurso[]
}

model ClaseBonus {
  id              String    @id @default(uuid()) @db.Uuid
  titulo          String    @db.VarChar(255)
  descripcion     String?   @db.Text
  linkVideo       String    @db.VarChar(255)
  fechaDisponible DateTime?
  orden           Int
  planId       String    @db.Uuid
   
  plan         Plan     @relation(fields: [planId], references: [id])
}

model Recurso {
  id      String @id @default(uuid()) @db.Uuid
  titulo  String @db.VarChar(255)
  orden   Int
  temaId  String @db.Uuid
  tema    Tema   @relation(fields: [temaId], references: [id])
  
  // Relations to other content models
  video    Video?
  pdf      Pdf?
  preguntas Pregunta[]
  
  // Progress
  progreso ProgresoRecurso[]
  
  @@unique([temaId, orden])
}

model Video {
  id        String  @id @default(uuid()) @db.Uuid
  url       String  @db.VarChar(255)
  recursoId String  @unique @db.Uuid
  recurso   Recurso @relation(fields: [recursoId], references: [id], onDelete: Cascade)
}

model Pdf {
  id        String  @id @default(uuid()) @db.Uuid
  url       String  @db.VarChar(255) // URL to the PDF file
  recursoId String  @unique @db.Uuid
  recurso   Recurso @relation(fields: [recursoId], references: [id], onDelete: Cascade)
}

model Pregunta {
  id        String  @id @default(uuid()) @db.Uuid
  texto     String  @db.VarChar(255)
  opciones  Json    @db.JsonB
  respuesta String  @db.VarChar(255)
  orden     Int
  recursoId String  @db.Uuid
  recurso   Recurso @relation(fields: [recursoId], references: [id], onDelete: Cascade)

  @@unique([recursoId, orden])
}

model ProgresoRecurso {
  usuarioId   String   @db.Uuid
  recursoId   String   @db.Uuid
  
  completado  Boolean  @default(false)
  completadoEn DateTime? @db.Timestamptz
  
  usuario     usuarios  @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  recurso     Recurso   @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  
  // Clave compuesta para asegurar que cada usuario solo pueda tener un registro por recurso
  @@id([usuarioId, recursoId])
}